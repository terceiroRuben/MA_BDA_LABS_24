package rplaces

object Types {
    type ColorIndex = Int
    type CoordX = Int
    type CoordY = Int
    type UserId = Long
    type TileId = Int
    type ColorRgb = Int // Integer representation of a RGB color. Used to render an image.
    type ColorHex = String // Hexadecimal representation of a RGB color
}
